<resources>
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="labelTargetList">
        <item name="android:layout_marginStart">@dimen/margin_start_target_selection</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/colorLabelTargetList</item>
        <item name="android:textSize">@dimen/textSizeLabelTargetList</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Transparent" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>

    </style>

    <style name="TargetSelectionList">
        <item name="android:orientation">horizontal</item>
        <item name="android:layout_marginTop">@dimen/margin_top_list_target_selection</item>
        <item name="android:layout_marginStart">@dimen/margin_start_target_selection</item>
    </style>

    <dimen name="margin_top_list_target_selection">16dp</dimen>
</resources>
